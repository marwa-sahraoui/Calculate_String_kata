package fr.arolla;

public class CalculateString {

    public static int add(String number) {
        if(number.isEmpty()){
            return 0;
        }
         return Integer.parseInt(number);
    }
}
